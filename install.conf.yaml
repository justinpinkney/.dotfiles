- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux.conf
    ~/.config/fish/config.fish: config.fish
    ~/.todo/config: todo.cfg
